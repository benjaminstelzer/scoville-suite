# WordPress suite integration

W-024, 2026-09-22. Imported the clean WordPress source at
`583288b7f89bbdee011f3bc302bc210c1d989c71` from the retained legacy checkout.
The original checkout and its Git history are unchanged. Member development
records remain historical, not a second active planning owner.

## Result

- Canonical member and package: `scoville-wordpress-ui-backend-anti-ai-slop`.
- `suite.json` owns exports, family order and README composition. WordPress
  follows UI. UI discovery now names the new specialist.
- Shared family and install fragments generate every member README. The suite
  reuses the new member's introduction and keeps Workflow first and Codex-only.
- WordPress technical references are byte-identical to the imported source.
  Package changes are identity, UI metadata and family ownership context.
- Public prose follows the existing member structure, with a WordPress-specific
  opening, scope, usage, compatibility, installation, rules and sources.
  Runtime limitations remain explicit. No live WordPress proof is claimed.

## Checks

Build: `E:/Dropbox/AI Projects/temp/2026-09-22-wordpress-suite/build-r1`.
Contains ten packages and 189 files, including the suite-only Workflow.
Source-package comparison and exact inventory/hash verification pass.

Receipt SHA-256:
`6c7b2863c70ec110746fd672efec151f9bcb0797ff1125d565f01c70ed3310ee`.
WordPress core SHA-256:
`eaf369cc7fe9d460c7c0c8dc8e4eb9ad469a73eeb5b7a8a9f1496a51cd3c930f`.

Five build tests pass, including family membership, standalone files and
UI routing metadata. Sixteen existing CLI-runner tests also pass.
README projections match their sources. The old quick validator rejects the
supported `compatibility` field. Separate YAML validation confirms name,
description and the 353-character compatibility field without removing it.

The author independently accepts wp-01 through wp-05 after reading every final
answer, checking original events and all eleven native Luna/medium contexts,
and comparing five frozen prompt hashes and delivered reference hashes.
No tools ran. Stderr is empty. The only diagnostic is the prequalified
disabled-Code-Mode message. SOL's independent grades are in
`luna-tests/wordpress-sol-results.md`. No correction or retry was required.
These are theoretical comprehension tests, not live UI acceptance.

Spacing is a priority acceptance concern, as reinforced by the user. wp-01
tests preservation of native Core margins instead of an arbitrary 16px scale.
wp-02 tests parent-owned gaps, no duplicate child margins, no hidden-child gap
slot and stable Core Flex's numeric `gap = pixels / 4`. The answer states all
four rules. A real-project check must still measure actual geometry after
source correction and view the rendered result. These answers cannot prove
pixel-correct runtime spacing.

The exact repeat procedure is `luna-tests/wordpress-execution.md`. Cases and
hidden keys were fixed before execution. Raw evidence remains in task-temp.

## Public profile and release boundary

Profile commit `f9a915c3da5ad020b1004b31c986678cc0339089` is published and its
remote README was compared with the local bytes. It places Scoville Family
above Ask Family and WordPress under Scoville, without a WordPress section.

The subsequent user-requested release draft at
`E:/Dropbox/AI Projects/projects/BenjaminStelzer/README.md` already contains
the final WordPress, Scoville Suite, Ask Suite and Ask SOL URLs. That draft is
not pushed. Their future reachability remains a release check under W-025.
Current package and README sources likewise use final URLs. Historical links
and import provenance are not rewritten.

The canonical GitHub Skill now requires verified Plan Viewer release assets,
build provenance and uploaded-file hashes before old-release cleanup. Its suite
route invokes the same check. W-025 retains the later one-release/one-version-tag
audit. No tags or releases were deleted, and no Skill was published or installed.
W-009 remains user-paused.
