# Codex CLI comprehension results

`selected-cases.json` defines the current 45-case selection: five cases for each
of six Scoville Skills and one representative of each of three Ask types.

Earlier aggregate runs used a broader package inventory. Their receipts do not
identify the current candidate and therefore provide no release evidence for it.
Retained member-specific reports, such as `wordpress-sol-results.md` and the
Workflow result files, describe only their stated historical package bytes and
limits.

The current candidate has no complete 45-case result. Before release, run the
selected cases against the final unchanged packages, review semantic outcomes
independently and record the exact model, effort and receipt identities.
