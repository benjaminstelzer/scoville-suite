# Supplemental check — author key withheld

Created after initial answers omitted two required details; no initial answer
or grading feedback is given to the tester. Original keys remain unchanged.

## Prompt

In an otherwise ready coordinator executor-assignment path, give the ordered
execution-memory helper operations and the exact stored state immediately
before the native sender runs. State what remains if the native call throws,
whether it may be repeated, and which receipt fields can be displayed.

Separately, the coordinator resumes after compaction. Its former core and
result/review contents are no longer available; only their read markers/hashes
remain. The expected child turn has completed. Name the exact required reads
before validation, archival or acceptance, and distinguish them from unrelated
phase reads. Do not execute actions.

## Hidden author key

Prepare retains complete bytes once. Send checks fresh binding/guard/lifecycle,
reserves checking during async validation, then stores send_unknown before the
native call. Throw/rejection/ambiguity never permits resend. Display compact
target/role/reference/characters/bytes/SHA256/state; do not expose the prompt.
Reload operations.md core and currently required wait/results/review rules;
before acceptance load accepted/scope/rollover rules. Do not read all phases.
No transition based only on old read/hash markers.
