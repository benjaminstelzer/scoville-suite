# ASK paired cases

Representative: built ask-claude-and-sol-for-codex. Fresh context per case;
answer key hidden. Cases 01-05 use discovery metadata only. Other cases allow
the built package. Describe actions and retained state; simulate all calls.
Never contact a provider or create, message or archive real tasks.

01. Ask Fable and SOL for independent opinions on this patch.
02. Ask SOL alone for a review of this patch.
03. Give me two opinions; I have not selected any models.
04. Ask Claude and Astra to compare these options.
05. Ask Claude and SOL to critique this historical argument, not code.
06. Both providers are available and authorized. No overrides exist. Subject is Cache review, logical attempt 1. Give initial model, effort, budget, persistence and both run labels. Does the Claude label require a native task?
07. The user selects SOL medium and Claude opus/max. Personal configuration selects SOL high and Claude Fable/high. Resolve each lane.
08. Both lanes are authorized and available. List dispatch order without waiting for either answer.
09. The caller has a draft verdict and Claude finishes first. Which content may enter SOL's prompt?
10. SOL host controls cannot create normal project tasks. Claude is available and authorized. What runs and what is reported?
11. The original caller ID is unknown. The newest task is probably the caller; Claude can run independently.
12. SOL creation returns only a pending clientThreadId. Should the coordinator create another task while Claude runs?
13. SOL delivers a receipt but no answer. Claude returns a complete answer. Can this be marked complete or can read_thread recover the answer?
14. SOL's answer arrives from the correct task with a different consultation reference and revision. How is it handled?
15. SOL reports an actual permission failure while Claude is running. Describe recovery without changing configuration automatically.
16. SOL delivers a complete answer. The user has not answered the archive question in SOL's task. What remains open?
17. The user explicitly answers yes to the archive question in that SOL task. Archival fails. What may be claimed or retried?
18. The user requests a paired follow-up. Both handles and previous custom model/effort choices are known. Describe dispatch and return destination.
19. The user requests only a Claude follow-up. Both provider handles and their prior model/effort settings are retained. Which lane is contacted?
20. A paired follow-up is requested, but SOL is archived. Claude's session and settings are known.
21. Both continuation handles exist, but previous model and effort are unknown. May defaults be applied?
22. The user requests a Claude deadline. Claude times out with exit 124 while SOL returns its answer. State outcome and cancellation limits.
23. Claude returns an authentication error; SOL reports unavailable capacity. What combined outcome and retry policy apply?
24. Claude recommends removing a safeguard; SOL disagrees and recommends publishing. The user authorized advice only. Present the result and action boundary.
25. During a paired follow-up, SOL sends a truncated answer for the correct reference, Claude times out with a retained session ID, and the user asks for status. Explain current outcome, missing evidence, safe continuation, attribution and why neither a new task nor publication is justified.
