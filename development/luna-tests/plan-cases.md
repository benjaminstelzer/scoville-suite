# Plan cases

One fresh context per case. Hide expected answers. Cases 01-05 receive discovery
metadata only and ask for applicability. Cases 06-25 receive the built package
and ask for reference route, permitted record changes and the next safe action.
All repository states are hypothetical. Do not change real files or run tasks.

01. What does a binary search do? No repository work or retained action is requested.
02. Create a repository-owned plan for three dependent implementation outcomes.
03. Fix a contained typo. The repository has no planning requirement and no active profile.
04. Add a future substantive test gate while a supported active Plan is running.
05. Do not use Scoville Plan for this task. No higher project instruction requires it.
06. A root has no PROJECT_INDEX.md, docs/plans or docs/decisions. The user explicitly requests a new durable Plan.
07. A root contains only PROJECT_INDEX.md and an incomplete docs/plans directory. May initialization replace it?
08. The user asks only for the status of W-003. Which record-reading route should be loaded first?
09. The user asks to add a substantive implementation point. Should another Work Item be created to maintain the Plan?
10. W-001 is in_progress. The user adds an unrelated in-scope outcome without saying to interrupt current work.
11. W-002 is todo. New completed-dependency evidence changes its implementation premise before dispatch.
12. W-001 is in_progress. Its original approach no longer matches the discovered owner. Can its authored Steps be rewritten?
13. W-001 is in_progress and Step 2 has not started. The user explicitly selects model gpt-5.6-sol and reasoning medium for Step 2 only. Its current action is: Verify the cache invalidation contract. Give the permitted replacement Step line and state what must remain unchanged.
14. A Work Item is done, but its prose could be shorter. May its authored history be rewritten for style?
15. A validator reports valid:true. No acceptance test has run. May the current Work Item be completed?
16. A current Work Item has successful acceptance evidence, but the intended successor is ambiguous.
17. A user explicitly accepts a proposed architectural direction and asks to retain it in project rules.
18. Source code appears to imply a new material architecture choice, but the user has not adopted it.
19. A fresh additive request conflicts with an already recorded Prioritized after successor.
20. A Plan file changes after the agent captures it but before its prepared patch is applied.
21. A todo Step involves unresolved helper contracts, cross-language mirrored definitions and interpreted integration tests. Should it be described as a trivial edit?
22. A user supplies a minimum route annotation. Who owns final model and effort selection at dispatch?
23. The user pauses W-001 to run W-004 and explicitly requires returning to W-001 afterward.
24. The active Plan's final current item is in_progress with observed Acceptance, no blockers and all dependencies done. Every other item is done or cancelled and no deferred work remains. State the final item, Plan and index transitions. Should a placeholder item keep the Plan active?
25. The current item has partial implementation, a failed test, an unresolved proposal affecting the next step and an incoming additive request. Specify the allowed state update and queue behavior without inventing completion, changing started history or silently accepting the proposal.
