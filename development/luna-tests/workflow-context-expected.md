# Hidden author key — frozen before Terra execution

All criteria must pass. Never supply this key or grades to the tester.

## context-01

1. Reuse stored bytes; shortened receipt display causes no build. Fresh binding,
   exact activated guard and lifecycle checks precede one send; store unknown
   before call. Display target/role/reference/characters/bytes/hash/state only.
2. Block stale dispatch. Do not patch payload or blindly regenerate/resend.
   Reconcile the proven-unsent assignment through ordinary recovery.
3. No send; reconcile exact guard/activation, never infer authorization.
4. No activation/send of partial JSON; construction failure requires ordinary
   pending-writer cleanup and its proofs, unlike shortened display.
5. Inspect receipt only; counts remain one build/one send attempt. A send reply
   alone establishes neither child execution nor accepted completion.

## context-02

1–2. Uncertainty/missing memory is not not_sent. Reconcile exact task/reference/
turn evidence. Preserve child/guard, block replay/replacement, no invented
success or automatic archival/Plan advancement. No arbitrary new key.
3. Reuse complete payload after fresh binding/guard/lifecycle checks; reserve
unknown before one attempt; no rebuild.
4. Wait/results gates allow automatic byte-exact projection recovery. Preserve
changes_requested/findings; archive only after retained result and exact-state
proof, with separate review/repair/Plan gates.
5. Retain conflict and fail closed; no normalization, unconditional acceptance,
replacement reviewer or extra unbounded reads.

## context-03

1. Reuse wait rules; same child/cursor wait. No full reload, active-child chat
read or unchanged commentary.
2. Reload core and current wait/results/review routes before transition; old
hash/read marker is not content. No unrelated phase reload.
3. Load complete current affected rules/dependencies before dispatch.
4. Accepted/scope/rollover route and checkpoint precede selection. Coordinator
>=33 means rollover at equality.
5. Worker uses >66; equality continues. No threshold or polling changes.

## Limits

Mock-sender mechanics and Terra comprehension are separate. Neither proves
live send/archival, production token savings or Luna comprehension.
