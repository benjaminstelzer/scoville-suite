# Scenario scope clarification v4

Code r3 case 16 exposed ambiguity in the old v2 wrapper: it separated
hypothetical routes from evaluator reads while the Skill required references
before a substantive judgment. Preserve that run as inconclusive for reference
compliance, not an accepted pass or proven Skill defect. The same ambiguity
limits the earlier case-15 diagnosis; its original record remains historical.

For Code cases 06 onward, replace only the v2 scope paragraph with:

> Mode, Risk, Routes and Next describe the requested operation inside the
> hypothetical case, not the act of answering this evaluation. If the case
> itself asks only for classification or planning, preserve that boundary.
> Follow the supplied Skill's own rule for whether selected route text is
> required before returning the requested judgment. Do not add a route merely
> because you are answering, and do not skip a Skill-required read merely
> because no project action is executed. Do not execute the operation. Report
> hypothetical next actions without claiming they occurred.

Keep cases, keys, package bytes, model and packaged-text READ protocol fixed.
Supply no expected route or criterion-specific hint. Qualify on case 16, then
Core-only 06/12 and Validation-only 13/15 before continuing. This is transport
clarification, not proof of Skill improvement. Preserve prior accepted evidence
with its wrapper version; repeat affected reference-selection cases on v4.
