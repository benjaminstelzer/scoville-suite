# Workflow archive regression

Four targeted cases authorized after the 2026-09-22 archive fix. Fresh Luna
Medium context per case; SOL Medium grades with the separate hidden key.
Use the built Workflow core and requested packaged text. All states are
hypothetical; describe decisions and simulated calls, never execute them.
Project contract and coordinator identity gates have already succeeded.

## archive-23

After transfer and activation, the successor is exact-ID reachable under the
verified active guard; the predecessor's activation turn is authoritatively
complete. The successor is absent from the normal list, although a similarly
titled task is visible. Its exact native status was not returned. Describe
continuation and archival without inventing missing evidence.

## archive-24

The activated successor is present by exact ID and host in threads, absent
from pinnedThreads and every custom section's itemKeys; predecessor completion
and current guard capability are proven. Its title has the SCW prefix. Archival
returns only completed, without archived:true. What is established and may the
associated transition advance?

## archive-active-chain

G30 is at an accepted writer-free boundary. Fresh capability=plan verification
confirms its exact workflow, ID and generation, coordinator_active state,
writer:null and rollover:null. A fresh exact-ID host read reports that same ID
and host with thread.status.type=active. The actual list response has complete
threads, pinnedThreads and sections arrays and no unavailable hosts or sources.
G30 is absent from threads, pins and section task keys. Its user-facing status
is retained. The retained G27-to-G30 archive chain has consecutive generations,
matching IDs/hosts and authoritative completed activation-turn evidence.
Archive receipts are empty. What inputs, helper operation, actions and proof
are needed? May the missing list entry defer all predecessor archival?

## archive-negative

Use separate variants of an activated exact-ID-reachable successor omitted
from threads. Other handoff and status-retention proofs are valid unless the
variant changes them. List arrays are complete with no availability errors.
For each variant state whether continuation and predecessor archival are
allowed, and the next safe action. Do not combine variants.

1. Fresh exact host status is idle; the guard says coordinator_active.
2. Fresh exact host status is active, but a section contains its exact task key.
3. Fresh exact host status is active, but unavailableHosts contains local.
4. Fresh exact host status is active, but the exact predecessor activation turn
   is still inProgress.
