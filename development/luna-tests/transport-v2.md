# Evaluation transport v2

Author-approved clarification after code-08. Original cases, keys, packages and
evaluation-manifest.json remain unchanged. Preserve every v1 run and SOL grade.

For case 06 onward, append this neutral scope definition to the wrapper:

> Mode, Risk, Routes and Next describe the requested operation inside the
> hypothetical case, not the act of answering this evaluation. If the case
> itself asks only for classification or planning, preserve that boundary.
> Routes names references required for that hypothetical operation. It does not
> mean references required merely to classify it. Do not execute the operation.
> Read the built core first, then only references needed for your evaluation
> answer. Report hypothetical next actions without claiming they occurred.

No model hints, route answers or criterion-specific coaching may be added.
Record this file's SHA-256 and transport_version=2 in each new run's metadata.
Continue using frozen package/test hashes and fresh Luna Medium contexts.

Rerun code-06–08 in separate `code-NN-v2` evidence directories before code-09–25.
Code-06 v1 remains valid; its rerun checks that classification stays Core-only.
Code-07 v1 is not accepted: native tools loaded Planning unnecessarily and read
all references with the core before selecting routes. Its correct final answer
does not erase this behavior. Code-08 v1 is inconclusive: its wrapper did not
distinguish scenario routes from classifier routes. Retain SOL's original fail
and this author disposition; do not report it as a proven Skill defect.

For all cases inspect actual tool calls as well as final answers. Check package
reads, unnecessary references, forbidden actions and key/source isolation.
Do not infer correct routing from the final Routes field alone.
