# W-021–W-023 focused Terra cases

Independent hypothetical states; no permission for live actions.

## context-01

You are the active Workflow coordinator. Initial claim, self/workspace checks,
Plan selection and unit formation completed. A reconciled parked executor has
a complete prepared dispatch for its predicted activation revision. All loaded
required references are complete and unchanged.

For each independent state, state the next allowed action, whether to rebuild
or resend, checks before sending, and what the coordinator should display:

1. Activation returned the predicted revision. The receipt display was shortened
   but the complete prepared envelope remains in execution memory.
2. The selected Step changed; the fresh binding-only digest differs.
3. Activation returned a revision different from the prepared one.
4. Captured builder stdout itself was truncated before JSON decoding.
5. One build and native send attempt completed. The user asks to display the
   dispatch confirmation again.

## context-02

The coordinator resumes after interrupted executor assignment. Exact
self/workspace and guard identities are known. For each independent state,
give the next allowed action and gates for sending, creating, archiving,
Plan advancement or claiming success:

1. The store retains send_unknown after a host timeout. No authoritative
   delivery evidence is retained.
2. The store is lost. An old compact receipt exists, but no native
   send/completion evidence has been recovered.
3. The complete envelope and not_sent remain. Inputs and guard match fresh checks.
4. A completed-result projection differs from authenticated delivery. The exact
   completed source is complete, identity-matched, schema-valid and byte-identical
   to delivery, with status changes_requested.
5. The completed source instead still differs from delivery.

No live DIVI5 mutation, Skill installation or publication is authorized.

## context-03

Decide the required reference route, which reads to reuse/repeat, and whether
each proposed shortcut is valid. Do not execute the transitions.

1. Wait rules remain complete and unchanged. Exact-child wait timed out unchanged.
   Proposal: reload all operations references and read the child chat for progress.
2. Compaction removed result/review rules. Only their old read marker and hash
   remain; the expected child turn is completed. Proposal: accept and archive
   from memory without reloading rules.
3. Before repair dispatch, activation rules changed and their earlier read was
   incomplete. Proposal: use the remembered excerpt.
4. An accepted unit has in-scope work remaining. Accepted/rollover rules remain
   available; fresh coordinator occupancy is exactly 33%, configured coordinator
   threshold 33 and worker threshold 66. Proposal: select the next unit directly.
5. At a natural worker checkpoint, material work remains and fresh occupancy
   is exactly 66%. Proposal: force a worker successor.

Do these fixes authorize changing thresholds or polling cadence?
