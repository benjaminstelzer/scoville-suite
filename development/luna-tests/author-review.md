# Test author review

Drafts only. No model execution or release approval.

Reviewed against current built packages:

- Handoff: all 25 criteria checked against core and continuation template.
- Workflow: all 25 criteria checked against private built core, relevant
  Operations sections, setup and rollover-readiness contracts. Setup now
  explicitly tests end-after-install; archival failure blocks its transition,
  whereas missing list visibility alone permits the guarded continuation path.
- ASK single/paired: all 50 criteria checked against each built core, adviser
  role and shared lifecycle contract. Case 06 tests exact RUN labels; follow-up
  fixtures supply open-task and retained-setting prerequisites. Unknown settings
  deliberately differ: single omits overrides; paired stops for resolution.
- Plan: all 25 criteria checked against built core and relevant native contracts.
  Previously read installed contracts were reused only after exact hash equality;
  the built core differs only in Workflow's renamed identifier. Case 06 retains
  missing authoring inputs, case 13 requires an exact permitted annotation, and
  case 24 now starts from a valid pre-completion state.
- Design: all 25 criteria checked against core and direct expert index.
  Case 06 specifies visual rather than wording repair; case 15 requires separate
  inset/center calculations; case 17 uses the exact acceptance-state vocabulary.
  This checks these case oracles, not every specialist module in the package.
- UI: all 25 criteria checked against core and Framework/Quality/Validation.
  Case 20 now tests concrete unit behavior; case 22 explicitly supplies the
  polished-presentation premise needed for populated-state coverage.
- Scribe: all 25 criteria checked against core and Fidelity/Interface/Prose.
  Case 05 explicitly grades non-activation; cases 06/11/15 require concrete
  localization, claim-preserving correction and accessible-name output.
- Brainstorm: all 25 criteria checked against core and research composition.
  Cases 08/22 now provide literal constraint IDs; case 24 requires a concrete
  schema-valid artifact rather than only recalling the schema rule.
- Research: all 25 criteria checked against core and all four routed references.
  Cases 12/17 now require concrete bounded claims; unlike metrics do not imply
  disagreement or a meaningful average.
- Claude-only: all 25 criteria checked against core, defaults and adapter result
  handling. Cases 09 and 23 now supply their extra user/project constraints;
  those constraints are not claimed as universal Skill rules. Case 13 supplies
  known prior settings and requires explicit preservation.
- Code: all 25 criteria checked against built core and three references.
  Discovery cases no longer demand hidden reference names. Cases 14/21/24
  state fix authority, completed setup and non-live risk boundaries. Case 25
  explicitly requests High-risk handling, which adds Change independently of
  the later implementation mentioned in the plan.

All twelve case/key sets have received author review. This is not model evidence
or exhaustive coverage of every Skill rule. Next: reconcile final package builds
and receipts, prepare isolated execution, then freeze case/answer hashes before
SOL runs. Any post-freeze correction requires an
explicit test revision and rerun; never fit expectations to observed answers.
