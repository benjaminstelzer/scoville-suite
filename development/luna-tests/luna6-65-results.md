# Retained Luna comprehension baseline

The former aggregate run is not a result for the current six-member suite. Its
package receipt covered a different inventory, so the aggregate denominator and
release conclusion no longer apply.

The current selection is owned by `selected-cases.json` and contains 45 cases.
No complete run against the final current package bytes has been recorded. Keep
member-specific historical reports only within their stated package and runtime
boundaries. Do not use this file as release approval.
