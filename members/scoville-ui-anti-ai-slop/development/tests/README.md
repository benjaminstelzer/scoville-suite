# UI evaluation cases

The current theoretical cases and evaluator keys are maintained in
`development/luna-tests/ui-cases.md` and `development/luna-tests/ui-expected.md`
at the suite root. PLAN-0006 owns the pending practical comparison. Its runtime
results must remain separate from rule-comprehension checks.
