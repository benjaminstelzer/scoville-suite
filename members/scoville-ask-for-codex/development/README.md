# Scoville Ask development

Canonical sources are in the nested scoville-ask-for-codex directory. README fragments live in development/readme/scoville-ask-for-codex at the suite root. Shared lifecycle code is generated from shared/runtime; never edit its bundled copy.

Run `python -m unittest discover -s members/scoville-ask-for-codex/development/tests` from the suite root. Provider and host simulations are not live consultation evidence.
