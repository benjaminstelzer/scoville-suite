# Optional project guidance

Only on an explicit setup request, add a short project-owned AGENTS.md paragraph
explaining that an explicitly invoked Scoville Workflow uses one coordinator,
one assigned source writer at a time, read-only reviewers and coordinator-owned
Plan edits and commits. Preserve every unrelated project instruction.

This paragraph is optional. The role assignment carries the actual boundaries.
It neither starts Workflow nor authorizes unrelated tasks or project actions.
There is no mandatory installation check, generated signature or per-step probe.
