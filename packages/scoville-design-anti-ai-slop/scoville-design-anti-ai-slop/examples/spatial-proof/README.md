# Spatial and type teaching assets

Each SVG is the editable source of the same-name PNG. The six examples isolate
one relation mechanism: shared outside anchors, declared type roles, longest
content fit, a named optical correction, purposeful asymmetry, or a decoration
removal control. Values belong to the example and are not defaults. The
manifest binds source and render hashes and records the review scope.

